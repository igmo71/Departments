@using Departments.Components.Icons

@if (TreeItem != null)
{
    if (TreeItem.IsExpanded)
    {
        <ChevronDownIcon />
    }
    else
    {
        <ChevronRightIcon />
    }
}
@code {
    [Parameter] public TreeItem? TreeItem { get; set; }
}
